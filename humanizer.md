# 去 AI 味（普通对话）PRD

## 1. 背景

当前 DeepSeek V4 在普通对话中存在较明显的 AI 味，常见表现包括空泛总结、模板化抒情、过度排比、解释腔、高频口癖等。项目主要服务同人文创作者和角色扮演爱好者，这类用户对文本自然度、具体感和语气可信度更敏感。

本功能在设置中增加“去 AI 味（普通对话）”开关。用户主动开启后，普通对话会在后台增加一次“自检 + 精修”流程，最终只展示精修后的回复。

## 2. 目标

1. 降低普通对话输出中的 AI 味。
2. 默认关闭，关闭时行为与当前版本保持一致。
3. 开启前明确告知用户会额外消耗 token，并可能增加等待时间。
4. 仅作用于普通对话，不影响群聊、角色对话、HTML 预览模式。
5. 聊天历史只保存用户消息、最终回复和必要的思考内容，不保存初稿、自检过程和审稿意见。

## 3. 非目标

1. 不改群聊生成链路。
2. 不改角色对话生成链路。
3. 不改 HTML 预览模式生成链路。
4. 不做长文写作流水线，不引入大纲、审稿、重写等复杂多阶段流程。
5. 不展示自检报告。
6. 不做 token 增量精确统计，第一版只在文案提示会增加消耗。
7. 不增加强弱档位，第一版只保留一个开关。

## 4. 场景判断

当前代码中三类对话可以清晰区分：

1. 普通对话：tab 没有 `type` 字段。
2. 角色对话：`type === 'single-character'`。
3. 群聊：`type === 'group'`。

功能生效判断：

```js
const shouldHumanize =
  state.humanizeNormalChat === true &&
  currentTab &&
  !currentTab.type;
```

HTML 预览模式已有独立分支，不进入普通对话生成链路，因此不受本功能影响。

## 5. 设置项

设置面板新增：

```text
去 AI 味（普通对话）
```

辅助说明：

```text
开启后，普通对话会额外进行一次自检和精修，回复更自然，但会消耗更多 token，并可能稍慢。群聊和角色对话不受影响。
```

默认值：

```text
关闭
```

持久化：

```text
localStorage key: dsHumanizeNormalChat
value: "true" | "false"
```

开启确认弹窗：

```text
开启“去 AI 味（普通对话）”后，普通对话会额外进行自检和精修，因此会消耗更多 token，并可能增加等待时间。该功能不影响群聊和角色对话。

是否开启？
```

按钮：

```text
确认开启
取消
```

行为：

1. 用户确认：开关开启，写入 localStorage。
2. 用户取消：开关保持关闭。
3. 用户关闭：无需确认，立即关闭并写入 localStorage。

## 6. 后台工作流程

### 6.1 关闭功能

完全走当前逻辑：

```text
用户输入
-> 构建 payload
-> 流式请求 DeepSeek
-> 展示并保存 assistant 回复
```

要求：关闭开关时，行为、输出、token 消耗、历史结构与当前版本保持一致。

### 6.2 开启功能

仅普通对话走新流程：

```text
用户输入
-> 保存用户消息
-> 构建普通对话 payload
-> 第一轮生成初稿
-> 第二轮自检 + 精修
-> 展示最终稿
-> 保存最终稿
```

用户不可见：

```text
初稿
自检结果
编辑过程
第二轮 reasoningContent
```

用户可见：

```text
最终精修稿
如果深度思考开启：第一轮 reasoningContent
```

聊天历史保存：

```text
user: 用户输入
assistant.content: 最终精修稿
assistant.reasoningContent: 第一轮 reasoningContent（仅深度思考开启时）
assistant.history: 只保存最终稿版本，不保存隐藏初稿
```

## 7. 与深度思考的关系

如果用户同时开启：

```text
深度思考 + 去 AI 味（普通对话）
```

行为：

```text
第一轮：跟随深度思考开关，生成初稿
第二轮：关闭深度思考，仅做自检 + 精修
最终正文：展示第二轮精修后的最终回复
思考内容：展示第一轮生成初稿时的 reasoningContent
```

原因：

1. 深度思考适合第一轮解决问题和组织内容。
2. 第二轮是文本编辑，开启深度思考收益小但 token 消耗高。
3. 用户开启深度思考后预期能看到思考内容，因此采用方案 A：展示第一轮 reasoningContent。

产品取舍：

```text
reasoningContent 对应第一轮“生成初稿”的思考过程；
最终正文对应第二轮“自检 + 精修”的结果。
```

## 8. 第二轮上下文规则

第二轮精修请求不携带完整前文。

如果用户本次发送内容为 `M` 字符，第一轮请求携带前文为 `N` 字符，模型返回初稿为 `K` 字符，那么第二轮主要输入为：

```text
编辑规则 R + 本次用户输入 M + 第一轮初稿 K + 极少量必要约束 C
```

不应为：

```text
完整前文 N + 本次用户输入 M + 第一轮初稿 K
```

第一版采用更克制策略：

```text
第二轮只携带：编辑规则 + 用户本次输入 + 第一轮初稿
```

并通过规则约束模型：

```text
如果初稿中有依赖上下文的信息，不要擅自改动事实、人物、数字、时间、称谓和结论。
```

## 9. 失败与停止

场景 1：第一轮初稿失败

```text
不保存 assistant 回复，提示用户请求失败。
```

场景 2：第一轮成功，第二轮精修失败

```text
展示并保存初稿，同时提示：精修失败，已返回初稿。
```

场景 3：用户点击停止

```text
中止当前后台请求。
如果最终稿尚未生成，不保存 assistant 回复。
重新生成场景下，恢复原 assistant 消息，不留下空白版本。
```

## 10. UI 交互

开启去 AI 味后需要显示阶段状态：

```text
第一轮：正在生成...
第二轮：正在精修回复...
```

要求：

1. 第一轮可复用现有发送中状态。
2. 第二轮必须显示“正在精修回复...”占位内容。
3. 发送按钮保持“停止”能力。
4. 用户点击停止时中止当前请求。
5. 最终稿生成后，占位气泡替换为最终内容。

## 11. 精修 Prompt

第二轮 system prompt：

```text
你是中文写作编辑。你的任务是把模型初稿改成更自然、更少 AI 味的最终回复。

你会收到用户原始需求和一版初稿。

请先在内部检查初稿的问题，但不要输出检查过程。
然后直接输出精修后的最终回复。

最高优先级：
- 保留用户原始需求，不改变事实、人物、数字、时间、称谓和结论
- 不重新发散，不新增大设定，不把短回复扩写成另一篇文章
- 如果用户明确要求字数、格式、段落数、标题数、风格或禁用内容，必须优先满足
- 只输出最终回复，不输出审稿过程、修改说明、标题或客套话

编辑原则：
- 识别具体 AI 腔模式，而不是泛泛润色
- 改写问题句，而不是简单删除内容
- 保留用户意图、事实和信息密度
- 用具体动作、对白、场景细节替代空泛抒情
- 交付前按禁用清单自检；未通过时继续改写

重点检查：
- 空泛总结
- 套话
- 过度工整
- 低信息量情绪描写
- 翻译腔
- 宣传文腔
- “不仅是……更是……”
- “不只是……而是……”
- “真正重要的不是……而是……”
- “从某种意义上说”
- “在更深层次上”
- “这背后其实是”
- “仿佛整个世界……”
- “空气仿佛凝固了”
- “时间仿佛静止了”
- “整个世界都安静下来”
- “眼中闪过一丝复杂……”
- “眼神复杂”
- “眸光微动”
- “嘴角勾起一抹笑”
- “喉结滚动”
- “指尖微微收紧”
- “心口像被什么轻轻撞了一下”
- “这就够了”
- “这就已经足够了”
- “仅此而已”
- “无需更多言语”
- “一切尽在不言中”
- “答案已经很明显了”
- “剩下的，就交给时间”
- “或许，这就是……”
- “也许，这才是……”
- “有些话，说出口就变了味”
- “有些答案，不必说出口”
- “有些情绪，早已有了答案”
- “沉默，比任何语言都更有力量”
- “他没有回答，因为答案早就在眼神里”
- “她没有再问，因为她已经懂了”
- “别怕，我在”
- “我会一直陪着你”
- “你不用一个人扛”
- “慢慢来，不急”
- “没关系的”
- 解释心理多于展示动作
- 可以替换到任何场景里的泛化句子
- 频繁排比、三连句、三段式总结
- 字数误判，例如把“我喜欢你”说成“三个字”
- 偷懒截断，例如用户要求 2000 字但初稿明显只有几百字

禁用口癖处理规则：
- 上述表达默认禁止使用，除非用户原文明确要求保留，或角色设定必须这样说
- 如果初稿中出现这些表达，不要换一个近义词糊弄过去
- 必须改成更具体的动作、对白、场景细节或直接陈述
- 同一类表达在同一回复中不得反复出现

改写要求：
- 保留用户原意
- 不改变事实
- 不擅自改动初稿中依赖上下文的事实、人物、数字、时间、称谓和结论
- 不擅自新增大设定
- 不删除关键内容
- 用具体动作、环境、停顿、对白替代空泛抒情
- 如果原回复是问答或建议，保持清晰直接，不要强行文学化
- 禁止使用“这就够了”及其近义收束句，除非用户原文明确要求保留
- 避免机械排比；如果连续出现三个结构相同的句子，至少改掉一个
- 不要乱数字数；不确定字符数时不要写“这短短的 N 个字”
- 如果用户要求具体字数，最终回复必须尽量接近目标字数；不得明显缩水
- 如果初稿长度远低于用户要求，精修时必须补足内容，而不是只润色原有短稿
- 不要输出标题、说明、问题清单或修改理由
- 只输出最终回复

内部交付前自检：
1. 是否出现“这就够了”或同类收束口癖？出现则改掉
2. 是否出现同人/RP 高频模板动作或伪深沉总结？出现则具体化
3. 是否有连续排比或三连结构？有则打散
4. 是否写了不可靠的字数判断？有则删除或改成不计数表达
5. 是否满足用户指定字数/格式？不满足则补足
6. 是否仍有 AI 味套话？有则再改一轮
```

第二轮 user prompt：

```text
用户原始需求：
{{userText}}

模型初稿：
{{draft}}

请输出精修后的最终回复。
```

## 12. 实现步骤

1. `modules/state.js` 新增 `humanizeNormalChat` 状态，读取 `dsHumanizeNormalChat`。
2. `index.html` 设置面板新增“去 AI 味（普通对话）”开关。
3. `modules/settings.js` 绑定开关，开启时弹确认框，取消时回滚。
4. `modules/humanizer.js` 新增两轮后台生成函数。
5. `modules/chat.js` 在 `fetchAndStreamResponse()` 中判断普通对话 + 开关开启后走 humanizer 分支。
6. 重新生成跟随开关；开关关闭走原逻辑，开启且普通对话走“初稿 -> 精修”。
7. 第二轮不携带完整历史前文。
8. 第二轮失败回退初稿，手动停止不保存半成品。

## 13. 验收项

1. 默认关闭：普通对话、角色对话、群聊、HTML 模式均保持当前行为。
2. 开启确认：确认后持久化，取消后开关回滚。
3. 普通对话开启：显示“正在生成...”和“正在精修回复...”，最终只展示精修稿。
4. 群聊开启该开关：不进入 humanizer。
5. 角色对话开启该开关：不进入 humanizer。
6. HTML 预览模式开启该开关：不进入 humanizer。
7. 深度思考 + 去 AI 味：第一轮使用深度思考，第二轮不用深度思考，最终展示第一轮 reasoningContent + 第二轮最终正文。
8. 第二轮失败：展示并保存初稿，提示“精修失败，已返回初稿”。
9. 用户点击停止：中止请求，不保存半成品；重新生成场景恢复原消息。
10. 重新生成：跟随开关。
