**前置条件**

- 本项目使用 ES Modules（`<script type="module">`），**必须通过 HTTP 服务器访问**，不支持 `file://` 协议直接打开
- 推荐启动方式：`python3 -m http.server 8080`，然后访问 `http://localhost:8080`

**测试范围**

- 覆盖端：桌面端、手机端
- 覆盖模式：夜间模式、日间模式
- 覆盖类型：静态结构、模块化架构、UI/响应式、核心交互、异常分支、真实 API 冒烟

***

**一、静态与基础可用性**

- 页面可正常打开，首页无白屏、无卡死、无明显报错
- `index.html`、`styles.css`、`prompts.js`（全局非模块脚本）资源可正常加载
- `modules/init.js` 及其依赖的所有 ES 模块（共 16 个）可正常加载
- 页面首次进入时，核心区域都存在：侧边栏、聊天区、输入区、设置面板、导出弹窗、指令管理面板、角色卡管理面板、新建群聊面板、关于面板、捐赠面板
- 编辑器诊断无报错，浏览器控制台无阻塞级报错
- 本地 HTTP 服务可正常访问 `/`、`/index.html`、`/styles.css`、`/modules/init.js`

**二、模块化架构验证**

- 16 个 ES 模块文件均可通过 HTTP 正常加载（无 404、无 CORS 错误）
- 模块间所有 `import` 的符号在源模块中均有对应 `export`
- `core.js` 函数注册中心正常工作：`register`、`call`、`get` 三个接口可用
- `init.js` 中注册的 11 个跨模块函数（renderChat、rebindChatButtons、updateInputCounter、renderTabs、invalidateTabCache、getCharacterColor、getCharacterById、createCharacterChatTab、openCharacterSelectPanel、openCharacterPanel、openCreateGroupPanel）均可通过 `coreCall` 正确调用
- chat.js、tabs.js、character.js、groupchat.js 之间无循环 `import`（通过 core.js 间接调用）
- 调用未注册的函数时，`core.call` 输出 console.warn 而非崩溃

**三、主题与视觉一致性**

- 夜间模式下，页面文字、按钮、输入框、侧边栏、弹窗均清晰可见
- 日间模式下，页面文字、按钮、输入框、侧边栏、弹窗均清晰可见
- 日间模式下，所有蓝底按钮文字均为白色，视觉和用户消息蓝底风格一致
- 日间/夜间模式切换后，不出现局部颜色错乱、浅底浅字、白底白字
- 设置面板在日间模式下标题、输入框、下拉框、按钮、说明文案均可读
- 用户消息、DeepSeek 回复、消息底部 meta 信息在两种模式下都清晰可见
- 输入框聚焦后不出现刺眼白框，两种模式一致
- 底部输入区圆角卡片样式在两种模式下都自然，不突兀
- 关于面板、捐赠面板在日间模式下样式正确，文案和图片清晰可见

**四、响应式与布局**

- 手机端打开页面时，布局不溢出、不横向滚动
- 侧边栏在手机端可正常打开、关闭、覆盖层点击关闭
- 手机端从屏幕左边缘向右滑动可打开侧边栏，侧边栏打开时向左滑动可关闭
- 设置弹窗在手机端宽度正常，内容可滚动，不被裁切
- 手机端 API Key 输入框、复制按钮、保存按钮不挤压、不超出容器
- 手机端字号按钮组不换行错乱，点击区域正常
- 手机端底部输入区不遮挡聊天内容
- 手机端聊天区底部留白足够，最后一条消息可完整看到
- 手机端"滑到底部"按钮不与输入区重叠，位置明显高于输入区
- 桌面端输入区宽度可随页面变宽，不显得过窄
- 桌面端聊天内容不会被固定输入区遮挡
- 横竖屏切换后，主要布局不破坏

**五、API Key 与首次进入**

- 无 API Key 时首次进入会弹出 Key 输入面板
- 输入合法 API Key 后可保存并开始使用
- 输入非法 Key（格式错误）时会给出提示，不应误保存
- 输入过短的 Key（长度 < 20）时会提示"API Key长度过短，可能是无效的Key，请检查！"
- 设置页中可查看并修改 API Key
- 设置页一键复制 API Key 可正常复制
- 复制成功后按钮图标临时变为绿色对勾，1.5 秒后恢复
- API Key 失效时，发送失败后会弹出确认框"检测到API Key可能无效，是否立即修改？"
- 确认后自动打开设置面板，取消后无操作

**六、设置页功能**

- 点击"设置"按钮可打开设置页
- 点击设置页遮罩或关闭按钮可关闭设置页
- 日间模式开关在设置页可正常切换并持久化
- 字号切换在设置页可即时生效并持久化
- 五档字号预览区显示正确
- 对话记忆条数（所有对话）可切换为默认、200、100、50、自定义
- 选择自定义时，自定义输入框会出现
- 自定义记忆条数输入后立即生效并持久化
- 切换会话后，全局记忆条数仍然保持生效
- "显示 Token 预估"开关可正常切换并持久化
- 关闭 Token 预估后，输入框的字数/token 计数消失
- 关闭 Token 预估后，用户消息底部的 token 提示消失
- 关闭 Token 预估后，AI 消息底部的字数/token 统计消失
- 刷新页面后，所有设置项仍按上次选择恢复

**七、会话管理**

- 新建会话可成功创建空白会话
- 切换会话后，聊天区显示对应内容
- 会话标题为空时显示默认"对话 n"
- 会话重命名后，侧边栏标题更新
- 重命名为空时，恢复默认会话名称
- 重命名面板中按 Enter 键可直接保存
- 删除非当前会话可成功移除（使用浏览器原生 confirm 弹窗）
- 删除当前会话后，会自动切到其他会话
- 删除最后一个会话时，会自动创建一个新的空白会话
- 删除会话后不应出现空列表、卡死或活动会话丢失
- 首轮对话完成后，可自动生成简短标题
- 流式输出期间切换会话，消息不会串到其他会话（tab 锁定修复验证）
- 流式输出期间切换会话，AI 回复写回正确的原始会话
- 流式输出期间切换会话，不会给错误会话生成标题

**八、消息发送主链路**

- 输入文本后点击发送可成功发起请求
- `Ctrl+Enter`（Mac: `Cmd+Enter`）可发送消息
- `Enter` 键换行（textarea 默认行为）
- 输入框中使用回车换行后，发送的消息保留换行格式（不被 Markdown 吞掉）
- 空输入时点击发送不会报错
- 发送期间按钮会切换为"停止"
- DeepSeek 回复可正常流式展示
- 深度思考关闭时走普通模型
- 深度思考打开时走 `deepseek-reasoner`
- 深度思考按钮位置在输入区工具栏内，交互正常
- 输入区实时计数显示"字数 / 约 tokens"
- 清空输入框后，计数恢复为 `0 字`
- 输入框根据内容自动调整高度，最小 44px，最大 88px（桌面端 104px），超出后出现滚动条

**九、消息内容与消息操作**

- 用户消息可正常渲染
- DeepSeek 回复正文可正常渲染
- 存在 reasoning 时，"思考过程"折叠区可正常显示
- Markdown 内容可正常渲染
- 代码块、换行、列表等常见格式显示正常
- 用户消息可复制，复制后按钮图标临时变为绿色对勾
- DeepSeek 消息可复制，复制后按钮图标临时变为绿色对勾
- AI 消息底部显示"思考 x 字，正文 y 字，约 z tokens"的统计信息
- 用户最后一条消息可编辑
- 编辑用户消息后可重新生成
- 编辑消息时内容为空，点击保存会提示"消息内容不能为空！"
- 删除任意消息后列表正确刷新
- 删除消息后不会导致索引错乱或按钮错位
- 消息操作按钮排列：复制(right:6px) → 删除(right:36px) → 编辑/重新生成/回复(right:66px，三者互斥)

**十、停止生成与重新生成**

- 生成中点击"停止"可中断当前请求
- 手动停止后，消息状态会标记为"生成中断，可重新生成"
- 后台切走页面导致中断时，也会保留中断状态
- 中断后的最后一条 AI 消息可点击重新生成
- 重新生成后可形成版本历史
- 版本切换按钮可前后切换不同生成结果
- 切换版本后，正文、思考过程、状态信息同步更新

**十一、上下文与 token 提示**

- 用户消息下方显示"本次正文 x 字，约 y tokens；历史记忆约 z tokens；本轮输入共约 a tokens"
- 用户消息 token 提示在日间/夜间模式都清晰可见
- 删除历史消息后，旧用户消息底部 token 提示会实时刷新
- 修改全局记忆条数后，旧用户消息底部 token 提示会实时刷新
- 上下文上限提醒基于真实发送给模型的裁剪后消息计算
- 当上下文接近上限时，会出现提醒文案
- 提醒中的"总结指令"可复制，复制后图标变为绿色对勾
- 记忆条数限制只影响发送给模型的上下文，不破坏本地完整聊天记录

**十二、导出功能**

- 点击会话导出按钮可弹出导出弹窗
- 导出弹窗可取消关闭
- 可选择"下载所有对话"
- 可选择"仅下载 DeepSeek 回复"
- "导出思考过程"开关默认打开
- 开启"导出思考过程"时，导出文件中区分 `[思考过程]` 和 `[正文]`
- 关闭"导出思考过程"时，导出文件中不包含思考过程
- 空会话点击导出时会给出提示，不生成空文件
- 导出文件名可包含会话名，并自动处理非法字符

**十三、指令管理**

- 点击"指令管理"可打开面板
- 点击关闭按钮或遮罩可关闭面板
- 无指令时显示空状态提示
- 可新增指令
- 可编辑已有指令
- 可删除已有指令
- 可复制指令内容，复制后图标变为绿色对勾
- 可"新建会话并插入指令"
- 插入指令后会新建会话，并将内容放入输入框
- 指令内容为空时保存会提示
- 指令优化前会先弹确认框
- 指令优化时内容为空会提示"请先填写指令内容"
- 指令优化时无 API Key 会弹出 Key 输入面板
- 指令优化结果可预览"原内容 / 优化后"
- 可选择保留原内容或替换为优化结果

**十四、指令市场**

- 点击"指令市场"可打开面板
- 点击关闭按钮或遮罩可关闭面板
- 刷新按钮可随机切换指令，按钮点击后有旋转动画
- 连续刷新不会出现重复指令
- 指令市场数据来源于 `prompts.js` 全局脚本中的 `MARKET_PROMPTS` 变量（非 fetch prompt.txt）
- 点击"新建对话并插入指令"可将当前指令插入输入框并新建会话
- 点击"保存进指令管理"可将当前指令保存到本地指令列表
- 指令内容为空时点击"保存进指令管理"或"新建对话并插入指令"会提示"指令内容不能为空"
- 智能生成：点击"智能生成"按钮可打开生成面板
- 智能生成：输入关键词后点击生成，调用 DeepSeek API
- 智能生成：生成中显示 loading 状态（spinner + "生成中..."文字）
- 智能生成：生成成功后自动关闭生成面板，结果显示在指令市场
- 智能生成：生成失败时显示错误提示
- 智能生成：无 API Key 时点击生成会弹出 Key 输入面板
- 智能生成：输入为空时提示"请输入关键词或描述"
- 智能生成：60 秒超时控制

**十五、空对话提示**

- 新建单聊会话后，聊天区中央显示"不知道跟DS老师聊什么？看看指令市场！"提示
- 新建群聊会话后，空白页**不显示**空对话提示
- 新建角色对话会话后，空白页**不显示**空对话提示
- 发送第一条消息后，空对话提示消失
- 点击"看看指令市场！"链接可打开指令市场面板
- 切换到有消息的会话时，空对话提示不显示
- 切换回空的单聊会话时，空对话提示重新出现
- 切换到空的群聊或角色对话会话时，空对话提示不显示

**十六、关于面板与捐赠面板**

- 点击"关于本站"按钮可打开关于面板
- 关于面板中显示站点介绍文案
- 关于面板中显示二维码图片（需验证图片可加载）
- 点击"支持与捐赠"按钮可打开捐赠面板
- 捐赠面板中显示感谢文案和支付宝收款码图片
- 两个面板均可通过关闭按钮或遮罩关闭
- 两个面板在日间模式下样式正确

**十七、滚动与聊天区行为**

- 长对话下，聊天区可正常滚动
- 不在底部时，"滑到底部"按钮出现
- 已接近底部时，"滑到底部"按钮隐藏
- 点击"滑到底部"按钮可平滑滚动到底部
- 流式输出时，如果用户当前就在底部，聊天区会自动跟随
- 流式输出时，如果用户已手动上滑浏览历史，不应强制抢回滚动位置
- 流式输出完成后（finalizeMessage 触发 renderChat），如果用户已上滑阅读中间内容，不会强制滚动到底部
- 群聊中角色消息创建时，如果用户已上滑，不会强制滚动到底部

**十八、搜索功能**

- `Ctrl+F`（Mac: `Cmd+F`）可打开搜索框
- 搜索框输入关键词后可匹配用户消息和 AI 回复正文
- 搜索可命中 AI 思考过程（reasoning）中的内容
- 搜索结果可上下导航（上一个/下一个）
- 搜索结果为空时显示"未找到匹配结果"
- 搜索高亮在聊天区中正确标记匹配位置
- 按 ESC 可关闭搜索框，关闭后无残留高亮
- 搜索激活时聊天区域增加顶部内边距，搜索结果栏不遮挡消息

**十九、快捷键与通用交互**

- ESC 键可关闭：编辑面板、重命名面板、确认弹窗、指令优化预览、指令管理、指令市场、智能生成面板、角色卡管理面板、新建群聊面板、角色选择面板、搜索框、关于面板、捐赠面板、导出弹窗
- `Ctrl+F` 打开搜索（替代浏览器默认查找）
- `Ctrl+Enter` / `Cmd+Enter` 发送消息
- 重命名面板中 Enter 键可直接保存

**二十、边界与异常流程**

- 页面在没有任何会话内容时能正常工作
- localStorage 中旧数据结构仍可兼容读取
- 老会话数据中存在旧 `memoryLimit` 字段时不影响当前逻辑
- 网络异常时，页面不会整体卡死
- API 返回错误时，会显示错误信息
- 失效 Key、网络错误、后台切回等异常路径下，不应破坏已有会话内容
- 刷新页面后，会话、设置、指令、主题等本地数据可恢复
- 多次快速切换设置项，不应导致明显状态错乱

**二十一、存储用量显示与保护**

- 侧边栏底部显示"本地存储容量 xxx/5MB(x%)"文案
- 存储用量在发送消息、保存指令、保存 API Key 后自动更新
- 存储用量 < 95% 时显示灰色文字，无感叹号图标
- 存储用量 ≥ 95% 时文字变红，出现红色感叹号图标
- 点击感叹号图标弹出提醒："当前聊天内容接近本地存储上限，请及时导出并清理过期会话。"
- 存储用量 ≥ 99% 时，点击发送按钮弹出阻止提示，不允许发送新消息
- 存储用量 ≥ 99% 时，按回车同样被阻止
- 存储用量在日间模式下正常显示（灰字/红字适配）

**二十二、数据损坏自动修复**

- `dsTabs` 数据损坏时，页面自动修复并刷新，不会白屏
- `dsTabs` 数据损坏时，修复逻辑会尝试补齐缺失字段（title、memoryLimit）
- `dsTabs` 数据彻底损坏时，清除并重置为空白状态
- `dsPrompts` 数据损坏时，自动重置为空数组，指令管理功能正常
- `MARKET_PROMPTS` 加载失败时，指令市场按钮不崩溃，显示空列表

**二十三、CDN 资源加载失败保护**

- `marked.js` 加载失败时，聊天消息降级为纯文本显示，不崩溃
- `DOMPurify.js` 加载失败时，聊天消息降级为纯文本显示，不崩溃
- CDN 加载失败后，发送消息、流式输出等核心功能仍可正常使用
- `tailwindcss` CDN 加载失败时，页面不完全崩溃（功能可用但样式降级）

**二十四、请求超时控制**

- 聊天请求 120 秒无响应后自动中断，显示"请求超时，请检查网络后重试"
- 指令生成请求 60 秒无响应后自动中断
- 超时中断后，发送按钮恢复正常状态（不再显示"停止"）
- 正常请求（非超时）不受影响

**二十五、Tab DOM 缓存与性能优化**

- 切换会话后，聊天区正确显示目标会话内容
- 切换会话后，消息的复制、编辑、删除、重新生成按钮均可正常点击
- 切换会话后，版本切换按钮（❮ ❯）可正常使用
- 切换会话后，token limit 提醒中的"复制指令"按钮可正常使用
- 在会话 A 中删除消息后，切到会话 B 再切回 A，显示的是删除后的最新内容
- 在会话 A 中切换版本后，切到会话 B 再切回 A，显示的是切换后的版本
- 搜索功能执行后，Tab DOM 缓存被正确清除，搜索结果正常显示
- 关闭搜索后，聊天内容正常恢复（无残留高亮）

**二十六、Markdown 渲染缓存**

- 相同内容的消息多次渲染时，显示结果一致
- 搜索高亮模式下，缓存不影响高亮显示的正确性
- 长对话场景下，切换会话的响应速度明显快于全量渲染

**二十六B、高优先级性能优化测试**

- 消息按钮使用事件委托：点击任意消息的复制、编辑、删除、重新生成、回复按钮，功能均正常工作
- 消息按钮使用事件委托：长对话（50+条消息）时，所有按钮功能正常，无点击无响应
- localStorage 写入防抖：快速连续发送多条消息（300ms 内），只执行一次 localStorage 写入
- localStorage 写入防抖：快速连续保存多个设置项（300ms 内），只执行一次 localStorage 写入
- localStorage 写入防抖：300ms 后，数据成功持久化到 localStorage
- 页面关闭立即保存：刷新页面前，300ms 内的数据变更会在 beforeunload 时立即刷入 localStorage，不丢失
- 页面关闭立即保存：关闭标签页前，300ms 内的数据变更会在 beforeunload 时立即刷入 localStorage，不丢失

**二十七、用户行为追踪**

- 页面加载时会自动生成 `ds_user_id` 并写入 localStorage
- 刷新页面后 `ds_user_id` 保持不变
- 页面加载时会发起一次 webhook 请求（访问事件）
- 发送消息时会发起一次 webhook 请求（发送事件）
- webhook 请求失败不影响正常功能

**二十八、真实 API 冒烟测试**

- 使用真实 Key 发起最小 `stream: false` 请求，验证返回内容和 `usage`
- 使用真实 Key 发起最小 `stream: true` 请求，验证 SSE 分块可收到
- `deepseek-chat` 普通请求可用
- `deepseek-reasoner` 流式请求可用，`reasoning_content` 和 `content` 字段正确分离
- 使用最短输入和较小 `max_tokens`，避免额外消耗过多额度

**二十九、角色卡管理**

- 侧边栏显示"角色卡管理"按钮，点击可打开角色卡管理面板
- 点击关闭按钮或遮罩可关闭角色卡管理面板
- 无角色时显示空状态提示"还没有创建任何角色"
- 点击"+ 新建角色"可打开角色编辑表单
- 角色编辑表单包含：名字、一句话摘要、性格、背景经历、外貌描写、说话风格、口头禅
- 角色名字为必填项，为空时保存会提示
- 填写完整信息后点击"保存"可成功创建角色
- 创建成功后角色列表刷新，显示新角色
- 可编辑已有角色，编辑后保存更新
- 可删除已有角色，删除前弹出确认框
- 删除角色时，如果该角色被某个对话或群聊引用，则弹出提示阻止删除，列出引用的对话名称
- 删除角色时，如果该角色未被任何对话引用，正常弹出确认框后删除
- 角色列表显示角色名字和摘要
- 角色摘要最多显示 2 行，超出部分省略号截断
- 角色卡片操作按钮（编辑、删除）横排在卡片底部右对齐
- 角色卡片在日间模式下样式正确
- 角色卡片在手机端布局正常，不溢出
- AI 智能生成按钮位于简要描述输入框内部右下角
- AI 智能生成：点击后旁边文字标签变为"生成中"，按钮图标不变
- AI 智能生成：生成完成后文字标签恢复为"AI 生成"
- AI 智能生成：输入简要描述后点击"AI 生成角色卡"，调用 DeepSeek API
- AI 智能生成：生成成功后自动填充所有表单字段
- AI 智能生成：生成失败时显示错误提示
- AI 智能生成：无 API Key 时点击生成会弹出 Key 输入面板
- AI 智能生成：输入为空时提示"请先输入简要描述"
- 角色卡数据存储在 localStorage（key: `dsCharacters`），刷新页面后数据不丢失
- `dsCharacters` 数据损坏时，自动重置为空数组，不影响其他功能分省略号截断
- 角色卡片操作按钮（编辑、删除）横排在卡片底部右对齐
- 角色卡片在日间模式下样式正确
- 角色卡片在手机端布局正常，不溢出
- AI 智能生成按钮位于简要描述输入框内部右下角
- AI 智能生成：点击后旁边文字标签变为"生成中"，按钮图标不变
- AI 智能生成：生成完成后文字标签恢复为"AI 生成"
- AI 智能生成：输入简要描述后点击"AI 生成角色卡"，调用 DeepSeek API
- AI 智能生成：生成成功后自动填充所有表单字段
- AI 智能生成：生成失败时显示错误提示
- AI 智能生成：无 API Key 时点击生成会弹出 Key 输入面板
- AI 智能生成：输入为空时提示"请先输入简要描述"
- 角色卡数据存储在 localStorage（key: `dsCharacters`），刷新页面后数据不丢失
- `dsCharacters` 数据损坏时，自动重置为空数组，不影响其他功能

**三十、新建群聊**

- 侧边栏显示"新建群聊"按钮，点击可打开新建群聊面板
- 点击关闭按钮、取消按钮或遮罩可关闭新建群聊面板
- 新建群聊面板显示群聊名称输入框和角色选择列表
- 角色选择列表展示所有已创建的角色（名字 + 摘要）
- 角色可通过复选框多选
- 至少选择 2 个角色才能创建群聊，否则提示
- 角色不足 2 个时点击新建群聊会提示"至少需要创建 2 个角色"
- 群聊名称可不填，默认显示"群聊"
- 创建成功后自动切换到新群聊会话，侧边栏关闭
- 新建群聊面板中"管理角色卡"链接可打开角色卡管理面板
- 群聊面板在日间模式下样式正确
- 群聊面板在手机端布局正常

**三十一、群聊会话**

- 群聊会话在侧边栏显示 👥 图标前缀，与单聊区分
- 群聊会话可正常切换、重命名、删除（与单聊一致）
- 群聊会话聊天区顶部显示"群聊成员"标签，包含所有参与角色的彩色标签
- 每个角色有独立的颜色标识（红、蓝、绿、黄、紫等）
- 用户消息在群聊中显示为蓝色气泡（与单聊一致）
- 角色消息显示为灰色气泡，左侧有角色颜色边框
- 角色消息上方显示角色名字标签（带角色颜色）
- 角色消息可复制、可删除
- 群聊中不显示用户消息的 token 统计
- 群聊中不显示上下文上限提醒
- 群聊中不显示深度思考过程（reasoning\_content）
- 切换到群聊会话时，聊天区正确显示群聊消息
- 切换回单聊会话时，聊天区正确显示单聊消息（互不影响）
- 刷新页面后，群聊会话和消息数据不丢失

**三十二、群聊消息发送与编排**

- 在群聊中输入消息并发送，系统自动调用编排逻辑
- 发送期间按钮切换为"停止"
- 编排器根据用户消息判断哪些角色应该回答
- 至少有一个角色会回答（不会出现零回复）
- 每个角色回复最多 5 句话
- 角色回复内容符合其性格设定（傲娇角色说"哼"等）
- 多个角色回复时，依次显示（先角色A，再角色B）
- 未被选中的角色如果与已回复角色相关，会自动追问
- 追问判断合理：不相关时不追问，相关时追问
- 发送中点击"停止"可中断群聊生成
- 中断后已生成的角色消息保留，未生成的角色不显示
- 中断的角色消息标记为"生成中断"
- 群聊发送失败时显示错误提示（toast）
- 无 API Key 时在群聊中发送会弹出 Key 输入面板
- 群聊首轮对话后可自动生成会话标题
- 群聊消息支持 Markdown 渲染
- 长时间群聊后，历史对话正确传递给编排器

**三十二B、群聊回复特定角色**

- 群聊中角色消息上悬浮显示回复按钮（↩ 图标）
- 点击回复按钮后，输入框上方出现引用条，显示"回复 @角色名：原文摘要..."
- 引用条右侧有 × 按钮可取消回复
- 取消回复后引用条消失，输入框恢复正常状态
- 发送消息后引用条自动消失
- 用户消息气泡内顶部显示引用条（蓝色左边框 + 角色名 + 原文摘要）
- 被回复的角色必须回答（排在第一个生成）
- 其他角色由 LLM 路由判断是否参与（被回复角色不会被重复选中）
- 不回复任何角色时（普通发言），行为与之前完全一致
- 回复功能仅在群聊中生效，单聊和单角色聊天不显示回复按钮
- 引用条在日间模式下样式正确（背景、文字颜色、边框）

**三十三、群聊与单聊兼容性**

- 新建的单聊会话（type: single 或无 type 字段）走原有逻辑
- 已有的单聊会话不受群聊功能影响，正常使用
- 单聊的所有功能（深度思考、版本历史、消息编辑、token 统计等）在单聊中正常
- 侧边栏同时显示单聊和群聊会话
- 删除群聊会话不影响单聊会话，反之亦然
- 旧版数据（无 type 字段的 tab）默认作为单聊处理，兼容不受影响

**三十四、LLM 通用调用封装**

- `callLLM` 函数支持流式和非流式两种模式
- `callLLMJSON` 函数可解析 LLM 返回的 JSON 格式内容
- 编排模块的路由判断使用 `callLLMJSON`，解析角色编号数组
- 编排模块的角色生成使用 `callLLM` 流式模式
- 编排模块的追问判断使用 `callLLM` 非流式模式
- 现有的指令优化、智能生成、标题生成等功能仍正常工作（未受重构影响）

**三十五、新建对话下拉菜单**

- 右上角 `+` 按钮点击后弹出下拉菜单，包含"新建对话"、"新建群聊"和"新建角色对话"三个选项
- 点击"新建对话"创建单聊会话，下拉菜单关闭
- 点击"新建群聊"打开新建群聊面板，下拉菜单关闭
- 点击"新建角色对话"打开角色选择面板，下拉菜单关闭
- 点击页面其他区域，下拉菜单自动关闭
- `+` 按钮和搜索按钮对齐
- 下拉菜单在日间模式下和夜间模式样式正确，展示清晰

**三十五B、新建角色对话（下拉菜单入口）**

- 无已创建角色时，点击"新建角色对话"弹出 toast 提醒"还没有创建任何角色，请先去角色卡管理中创建角色"
- 只有 1 个角色时，点击"新建角色对话"直接创建单角色聊天会话（不弹选择面板）
- 有 2 个及以上角色时，点击"新建角色对话"打开角色选择面板
- 角色选择面板显示所有已创建角色的名字和摘要
- 点击某个角色后创建单角色聊天会话，面板关闭
- 角色选择面板可通过关闭按钮、遮罩点击、ESC 键关闭
- 角色选择面板在日间模式下样式正确（背景、边框、文字颜色、hover 效果）

**三十六、群聊编辑与重新生成**

- 群聊中编辑用户消息后保存，正确截断后续消息并重新触发编排
- 群聊中编辑用户消息后，走群聊发送逻辑（不触发单聊的 fetchAndStreamResponse）
- 群聊中编辑保存后，isSending 状态正确管理，不会卡死
- 群聊中编辑保存后，停止按钮可以正常中断生成
- 单聊中编辑用户消息后，inputMeta（token 统计）正确更新
- 编辑非最后一条用户消息时，正确截断后续所有消息

**三十七、群聊停止与中断**

- 群聊生成过程中点击"停止"按钮，立即中断所有 LLM 调用（路由、生成、追问）
- 中断后已生成的角色消息保留，未生成的不显示
- 中断的角色消息标记为"生成中断"
- 中断后 isSending 状态正确重置，可以继续发送新消息
- 路由判断被中断时，不会生成任何角色消息
- 角色生成被中断时，已完成的保留，当前中断的不保存
- 追问判断被中断时，已完成的保留

**三十八、群聊多轮互动**

- 用户发消息后，角色A回答 → 角色B回应 → 角色A再回应（多轮互动）
- 多轮互动最多 3 轮，不会无限循环
- 每轮最多一个角色回应
- 每个角色本轮最多说 2 次
- 任何一轮没人想说话时自动结束
- 多轮互动中角色能看到本轮其他角色已说的话（不重复）
- 多轮互动中停止按钮可以中断

**三十九、群聊导出**

- 群聊对话导出为 TXT 时，角色消息显示正确的角色名（不是"DeepSeek"）
- 用户消息显示为"我"
- 单聊消息导出不受影响

**三十九B、单角色聊天**

- 角色卡列表中每个角色有"单独聊天"按钮（聊天图标）
- 点击后创建新会话，tab 类型为 single-character，存储 characterId
- 点击后自动关闭角色卡管理面板和侧边栏
- tab 标题默认显示角色名（无自定义标题时）
- 单角色聊天不自动生成标题，tab 名称始终显示角色名（不会被动态生成的标题覆盖）
- 用户可手动重命名，重命名后显示自定义标题（覆盖角色名）
- tab 前显示 🎭 图标（与群聊 👥、单聊无图标区分）
- 聊天区顶部显示"正在与角色对话"+ 角色名彩色标签
- 发送消息时自动注入角色 system prompt（性格、背景、外貌、说话风格、口头禅）
- system prompt 通过 buildPayloadMessages 在 payload 最前面插入，不影响本地消息存储
- AI 回复以角色身份和风格回复（不是默认 DeepSeek 助手风格）
- AI 回复消息上方显示角色名标签（带角色颜色），左侧有彩色边框
- 流式生成过程中也显示角色名标签和彩色边框
- 流式完成后全量渲染（finalizeMessage）时角色名标签不丢失
- 支持深度思考模式（切换为 deepseek-reasoner）
- 支持编辑用户消息和重新生成
- 支持停止生成
- 支持版本历史切换
- 支持导出为 TXT（AI 回复显示角色名，不是"DeepSeek"）
- 删除角色时，如果该角色有单独聊天会话，弹出提示阻止删除（引用类型显示为"角色对话"）
- 删除角色后，已有对话中角色 system prompt 丢失（getCharacterById 返回 null），回退为普通对话
- 切换到其他 tab 再切回，消息正常显示（DOM 缓存正常）
- 单角色聊天的消息格式与单聊一致（用户蓝色气泡，AI 灰色气泡），不使用群聊的角色消息样式
- 单角色聊天支持 token 统计和上下文上限提醒（与单聊一致）
- 单角色聊天支持记忆条数限制（与单聊一致）
- 旧版数据（无 single-character 类型）不受影响

**四十、日间模式兼容**

- 角色卡管理面板在日间模式下样式正确（背景、边框、文字颜色）
- 角色卡编辑面板在日间模式下样式正确
- 新建群聊面板在日间模式下样式正确（标签文字、角色名字、角色描述均清晰可见）
- 新建群聊面板中角色选择 hover 时背景为浅灰色（非深色闪烁）
- 群聊消息在日间模式下样式正确（背景、边框颜色）
- 群聊角色消息左侧彩色边框在日间模式下正确显示（不被全局规则覆盖为灰色）
- 群聊角色标签在日间模式下样式正确
- 群聊头部和成员标签在日间模式下样式正确
- 群聊 tab 的 👥 图标在日间模式下正常显示
- Header 中 `+` 新建按钮和搜索按钮在日间模式下 hover 时文字清晰可见
- 搜索框关闭按钮在日间模式下 hover 时文字清晰可见（非白色）
- 下拉菜单（新建对话/新建群聊）在日间模式下 hover 时背景为浅灰色、文字为深色
- 关于面板、捐赠面板关闭按钮在日间模式下 hover 时文字清晰可见
- API Key 面板中链接（platform.deepseek.com 等）在日间模式下对比度足够
- 空对话提示中"看看指令市场！"链接在日间模式下对比度足够
- Token 上限提醒中复制按钮 hover 时文字在日间模式下清晰可见

**四十一、数据兼容与边界**

- 旧版数据（tab 无 type 字段）默认作为单聊处理
- 群聊中旧数据（role 为 assistant 的消息）正确渲染为角色消息样式
- 删除角色前，如果该角色被对话或群聊引用，弹出提示要求先删除相关对话，不允许直接删除角色
- 删除未被引用的角色后，引用该角色的群聊不会崩溃（getCharacterById 返回 null 被 filter 过滤）
- 删除角色后，群聊历史消息中的角色名仍能正确显示（因为保存在消息数据中）
- localStorage 容量监控包含角色卡数据（dsCharacters）
- dsCharacters 数据损坏时自动重置为空数组
- 群聊名称为空时默认显示"群聊"
- 新建群聊面板每次打开时清空之前的选择
- 新建群聊面板每次打开时清空之前输入的群聊名称

**四十二、背景信息功能**

- 输入工具栏显示"背景"按钮（笔形图标 + "背景"文字），位于"深度思考"按钮和字数统计之间
- "背景"按钮样式与"深度思考"开关一致（圆角胶囊、暗色背景、白色文字）
- "背景"按钮在所有会话类型（单聊、群聊、角色对话）中均可见
- 点击"背景"按钮可打开背景信息面板
- 背景信息面板包含"你的角色名称"输入框和"对话背景"文本域
- 角色名称输入框有占位提示"例如：旅行者、探险队长、旁白..."
- 对话背景文本域有占位提示"例如：故事发生在一个魔法学院..."
- 面板可通过关闭按钮、取消按钮、遮罩点击关闭
- 填写信息后点击"保存"，toast 提示"背景信息已保存"
- 保存后关闭面板，输入框内容清空
- 重新打开面板时，显示已保存的内容（可修改）
- 背景信息保存在当前 tab 数据中（userRoleName、storyBackground 字段）
- 切换到其他 tab 后再切回，已保存的背景信息不丢失
- 不同 tab 的背景信息互相独立
- 刷新页面后，背景信息不丢失
- 背景信息面板在日间模式下样式正确
- 背景信息面板在手机端布局正常
- ESC 键可关闭背景信息面板
- 对话背景文本域可拖拽调整高度（min-height: 80px, max-height: 200px）

**四十二B、背景信息注入 LLM**

- 单聊中设置了背景信息后，发送消息时 LLM 回复会体现背景信息（如称呼用户设定的角色名）
- 单聊中同时设置了角色名称和对话背景，两者都会注入到 system prompt
- 单角色对话中设置了背景信息，角色回复会融入背景设定
- 群聊中设置了背景信息，角色回复会融入背景设定（通过 groupContext 传递）
- 未设置背景信息时，对话行为与之前完全一致（不注入额外 system prompt）
- 背景信息作为 system prompt 注入，不受记忆条数限制影响（始终存在）
- 背景信息中包含特殊字符（换行、引号等）时，不会导致 prompt 构建错误

**四十三、空对话提示类型过滤**

- 新建单聊会话后，空白页显示"不知道跟DS老师聊什么？看看指令市场！"提示
- 新建群聊会话后，空白页**不显示**空对话提示
- 新建角色对话会话后，空白页**不显示**空对话提示
- 单聊发送第一条消息后，空对话提示消失
- 群聊发送第一条消息后，空白页保持空白（无提示）
- 角色对话发送第一条消息后，空白页保持空白（无提示）
- 切换 tab 时，空对话提示根据目标 tab 类型正确显示/隐藏
- 单角色对话的空对话提示中角色名显示正确（非"DS老师"）

**四十四、AI 生成角色卡动画**

- 角色编辑面板中，AI 生成按钮位于简要描述输入框内部右下角
- AI 生成按钮位置 bottom: 18px（相比之前下移 10px）
- 点击"AI 生成"后，按钮图标和"生成中"文字变为金色（#fbbf24）
- 生成中图标有旋转动画（2s 线性循环）
- 生成中文字有呼吸闪烁动画（1.5s ease-in-out 循环）
- 生成完成后，图标和文字恢复正常颜色
- 日间模式下生成中动画为深金色（#d97706）

